---
import Nota from '../../../views/nota.astro';
import { getNotas } from '../../../lib/content';

export const getStaticPaths = async () => (await getNotas('en')).map((nota) => ({ params: { slug: nota.slug }, props: { nota } }));
const { nota } = Astro.props;
---
<Nota lang="en" nota={nota} />
