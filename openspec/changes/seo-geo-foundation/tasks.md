## 1. Identidad canónica y `<head>`

- [x] 1.1 `astro.config.mjs`: `site: 'https://diputnam.com'`, `trailingSlash: 'always'`; crear `src/lib/seo.ts` con `absolute(path)`, `truncate(text, max, suffix)` y `ogLocale(lang)`; verificar con `npm run build` que `dist/` no contiene `localhost:4321` (`grep -r "localhost" dist/ | wc -l` = 0) y que las doce rutas siguen construyendo
- [x] 1.2 `SiteHead.astro`: `<link rel="canonical">` absoluto, `hreflang`/`x-default` absolutos, meta `robots` (`index, follow, max-image-preview:large, max-snippet:-1`, o `noindex` con la prop), y ampliar `check-dist.mjs` para exigir canonical absoluto igual a la ruta y `hreflang` absolutos; verificar que `npm run build` pasa y que un canonical relativo inyectado a mano hace fallar `check-dist` nombrando la ruta
- [x] 1.3 Open Graph y Twitter en `SiteHead` (D2): `og:type`, `og:site_name`, `og:title`, `og:description`, `og:url`, `og:locale` + `og:locale:alternate` si hay par, `og:image` + `width`/`height`/`alt`/`type`, `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`; props nuevas `kind`, `image`, `article`, `noindex`; verificar por `grep` en `dist/en/contact/index.html` que aparecen los 12 `og:*`/`twitter:*` con URLs absolutas y que `dist/index.html` no lleva `og:locale:alternate` distinto de `en_US`

## 2. Datos de organización y JSON-LD

- [x] 2.1 Sanity: `siteSettings.organization` (`legalName`, `foundingYear`, `description` localeText, `sameAs[]` url, `logo` image opcional) en `studio/schemaTypes/siteSettings.ts`; `nota.seo` (`title` ≤ 60, `description` ≤ 155, `image`) en `nota.ts`; seeds (`seed.ts`, `seed.en.ts`, `seed-local.ts`) con `organization.description` en es/en tomada del texto de misión, `sameAs: []`, y `_updatedAt` estampado en el dataset local; verificar `node studio/scripts/seed-local.ts` y que `studio:build` compila el schema
- [x] 2.2 `content.ts`: `Settings.organization`, `_updatedAt` en `notaFields` y `getUpdatedAt(id)` para singletons, `Nota.seo`; `images.ts`: `ogUrl(image)` con `fit=crop&crop=focalpoint&fp-x/fp-y` y `rect` (D5); verificar con un script en el scratchpad que `ogUrl` de una imagen con hotspot a la derecha produce `fp-x` > 0.5 y que `getSettings('en').organization.description` está en inglés con fallback a español
- [x] 2.3 `jsonLd()` en `seo.ts` (D3): `@graph` con `Organization#organization` (name, legalName, url, logo absoluto, description por idioma, `PostalAddress` con `addressLocality`/`addressCountry: BO`, `GeoCoordinates` desde `coordinates`, `ContactPoint` con `telephone`/`email`/`availableLanguage`, `sameAs` solo si no está vacío), `WebSite#website` (`inLanguage`), tipo de página (`WebPage`/`AboutPage`/`ContactPage`/`CollectionPage`) con `isPartOf`, `about`, `inLanguage`, `name`, `description`, y `BreadcrumbList` en interiores; emitir un `<script type="application/ld+json">` en `SiteHead` con `</` escapado; verificar parseando el JSON de `dist/putnam/index.html` y `dist/en/putnam/index.html` (misma `Organization`, distinta `description`) y pasando `dist/contacto/index.html` por el Rich Results Test sin errores
- [x] 2.4 `NewsArticle` en `nota.astro`: `headline`, `datePublished` (`date`), `dateModified` (`_updatedAt` o `date`), `inLanguage`, `image` (OG de la nota), `articleSection`, `author`/`publisher` → `#organization`, `mainEntityOfPage` → canonical, más `article:published_time`/`modified_time`/`section` en OG y breadcrumb Inicio › Noticias › nota; verificar que el JSON-LD de `dist/noticias/<slug>/index.html` contiene un `NewsArticle` con fechas ISO 8601 y que el Rich Results Test lo reconoce como artículo

## 3. Portadas OG e iconos

- [x] 3.1 `scripts/og-template.html` (1200×630, paleta, fuentes de `public/fonts`, mark de Putnam, nombre de página y tagline leído del diccionario) y `scripts/og.mjs` (Playwright, `deviceScaleFactor: 1`) que escribe `public/og/{cover,home,eredita,putnam,noticias,unete,contacto}-{es,en}.png`; script `assets:og`; verificar abriendo tres PNG y que cada uno mide 1200×630 (`file public/og/*.png`) y pesa < 150 KB
- [x] 3.2 Conectar `og:image` por página (D4/D5): estáticas → `/og/<page>-<lang>.png`; notas → `ogUrl(nota.seo.image ?? nota.image)` o `/og/cover-<lang>.png`; verificar por `grep` que `dist/eredita/index.html` referencia `https://diputnam.com/og/eredita-es.png` y que la nota sin imagen del seed referencia `cover-es.png`
- [x] 3.3 `scripts/icons.mjs` con `sharp` (D8): `favicon.ico` 32, `favicon.svg`, `apple-touch-icon.png` 180 con fondo crema, `icons/icon-192.png`, `icons/icon-512.png`, `site.webmanifest` (`name`, `short_name`, `lang`, `theme_color #f4eedf`, `background_color #f4eedf`, `display: browser`); borrar `public/favicon.png`; enlaces en `SiteHead` (`icon` svg+png, `apple-touch-icon`, `manifest`); verificar `npm run assets:icons`, que los seis archivos existen en `dist/` y que Chrome DevTools › Application › Manifest no reporta errores

## 4. Copy orientado a búsqueda

- [x] 4.1 Revisar `meta` en `src/i18n/es.ts` y `en.ts` con el patrón D9 (propuesta inicial — ES: home "Putnam · Desarrollos inmobiliarios en La Paz, Bolivia"; eredita "Ereditá · Departamentos en La Paz | Putnam"; putnam "La firma · Diseño, planificación y obra | Putnam"; noticias "Noticias · Avances de obra y novedades | Putnam"; unete "Únete al equipo · Trabaja en Putnam, La Paz"; contacto "Contacto · Putnam en San Miguel, La Paz" — EN equivalentes) y descripciones de 110–155 caracteres con oferta + lugar; añadir `ui.notFound` (título, texto, `goHome`, `contact`) y `ui.og.alt`; verificar con un script que todos los títulos miden 30–60 y las descripciones 110–155 en ambos idiomas, y que `test:i18n` sigue en verde
- [x] 4.2 Título/descripción de nota: `nota.seo.title ?? truncate(nota.title, 60 - ' | Putnam'.length) + ' | Putnam'`, `nota.seo.description ?? truncate(nota.excerpt, 155)`; verificar con la nota del seed de 95 caracteres que el `<title>` en `dist/` mide ≤ 60, termina en "| Putnam" y no corta palabra, y que rellenando `seo.title` en el dataset local se usa el override

## 5. Superficie de rastreo

- [x] 5.1 `public/robots.txt` (`User-agent: *` / `Allow: /`, agentes de IA nombrados explícitamente con `Allow: /`, `Sitemap: https://diputnam.com/sitemap.xml`) y `public/_headers` (D11); verificar `curl -I` en `astro preview` que `/_astro/*.css` responde `Cache-Control: public, max-age=31536000, immutable` (o, si preview no aplica `_headers`, que `wrangler dev` sí lo hace) y que `/robots.txt` contiene la línea `Sitemap:`
- [x] 5.2 `src/pages/sitemap.xml.ts` (D6): doce rutas + notas de ambos idiomas, `lastmod` desde `_updatedAt`, `xhtml:link` es/en/x-default; verificar que `dist/sitemap.xml` valida contra el XSD de sitemaps (`xmllint --noout --schema`) y que su conjunto de `<loc>` coincide con los HTML indexables de `dist/`
- [x] 5.3 `src/pages/llms.txt.ts`: `# Putnam`, resumen, secciones ES/EN con las seis páginas y una línea de descripción cada una (desde `ui.meta`), notas con fecha, contacto público; verificar `curl` en preview que responde `text/plain; charset=utf-8` y que la nota del seed aparece con su URL absoluta
- [x] 5.4 `src/pages/404.astro` (D7): header, bloques es/en, enlaces a home y contacto por idioma, `noindex`, script inline que elige idioma por `pathname`; verificar que `dist/404.html` existe, que `wrangler dev` responde `404` en `/proyectos/` con el texto en español y en `/en/projects/` con el bloque inglés visible, y que `sitemap.xml` no la incluye

## 6. Guardas y documentación

- [x] 6.1 `tests/seo-audit.mjs` (D10) y `postbuild` = `check-dist && seo-audit`, script `test:seo`; verificar que `npm run build` pasa en verde y que cada una de estas inyecciones a mano en `dist/` hace fallar el audit nombrando la ruta: segundo `<h1>`, `<title>` de 70 caracteres, `og:image` a un archivo inexistente, JSON-LD con coma sobrante, ruta HTML ausente en `sitemap.xml`
- [x] 6.2 `npm run build`, `test:mobile`, `test:perf`, `test:sweep`, `test:i18n` en verde con el dataset local; verificar que el DOM visible de las doce rutas no cambió (diff de `dist/` previo vs nuevo restringido a `<body>`: solo difiere el 404 nuevo)
- [x] 6.3 README: dominio y canonical, custom domains + `_redirects` para `www` en Cloudflare, `assets:og`/`assets:icons` cuando cambie marca o tagline, campos `organization`/`seo` en el Studio, `test:seo`, verificación en Search Console/Bing y envío del sitemap, equivalentes en Vercel; verificar que el usuario puede seguir la sección sin conocimiento previo

## 7. Publicación (requiere dominio y cuentas)

- [ ] 7.1 Tras el deploy en `workers.dev`: validar `/`, `/eredita/`, `/en/contact/` y una nota con el Rich Results Test, el Sharing Debugger de Meta y el Post Inspector de LinkedIn; verificar que muestran imagen 1200×630, título y descripción correctos y ningún error de datos estructurados
- [ ] 7.2 Con `diputnam.com` en Cloudflare: custom domains apex + `www`, `_redirects` de `www`, verificar `curl -I https://www.diputnam.com/eredita` → `301` a `https://diputnam.com/eredita/`, y `https://diputnam.com/proyectos/` → `404`
- [ ] 7.3 Usuario: verificar propiedad en Google Search Console y Bing Webmaster Tools, enviar `sitemap.xml`, crear/reclamar el Perfil de Negocio de Google de la oficina de San Miguel y añadir su URL y las redes a `organization.sameAs` en el Studio; verificar que Search Console muestra las doce URLs descubiertas y sin errores de `hreflang`

## Notas de implementación (2026-09-14)

- Verificado con el dataset local: build + `check-dist` + `seo-audit` en verde, `test:i18n`, `test:sweep`, `test:mobile`, `test:perf` en verde; el `<body>` de las 18 páginas es idéntico al build previo.
- `wrangler dev` sirve `404.html` con estado 404 (bloque inglés bajo `/en/`), aplica `_headers` y redirige `/eredita` → `/eredita/`.
- El mark se vectorizó procedimentalmente (`scripts/trace-mark.mjs`, 0 píxeles de diferencia a 800 px); `favicon.svg` es ese trazado.
- Pendiente de URL pública (grupo 7): Rich Results Test, Sharing Debugger y Post Inspector (2.3/2.4/7.1); `xmllint` no está instalado, el sitemap se validó parseando namespaces y estructura.
- La comprobación del manifiesto se hizo por JSON válido + iconos presentes (`seo-audit`), no en DevTools.
