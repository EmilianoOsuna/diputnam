## 1. Modelo de contenido en el Studio

- [x] 1.1 Definir el tipo `proyecto` en `studio/schemaTypes/pages.ts` (D1): `name`, `slug` único validado `a-z0-9-` con `isUnique`, `order`, `status`, `card` (imagen + texto) y las secciones `hero`, `intro`, `gallery`, `typologies`, `legal`, `cta` extraídas del singleton como constantes compartidas; recortar `eredita` a `hero`, `intro`, `projects` (kicker, título, texto) y `cta`; registrar el tipo en `index.ts` (fuera de `singletons`) y añadir "Proyectos Ereditá" en `structure.ts` tras "Ereditá (línea)" ordenado por `order`; verificar con `npm run studio:build` y abriendo el Studio que aparece la colección y que un slug duplicado o con mayúsculas bloquea la publicación
- [x] 1.2 Cambiar el objeto de tipología (D5): eliminar `image`, añadir `video` (`file`, `accept: video/mp4`, descripción con H.264 ≤ 5 MB ≤ 1280 px), `poster` (`localeImage`, obligatorio si hay video), `images` (array de `localeImage` con `caption`, `layout: grid`) y validación "video o al menos una imagen"; preview con `images.0`; verificar en el Studio que una tipología sin medios no se publica, que un `.mov` se rechaza y que con video sin póster se muestra el error
- [x] 1.3 Añadir `proyecto` (referencia opcional) a `documentoLegal.ts` (D4) con la descripción "Vacío = se muestra en todos los proyectos" y mostrarlo en el preview; verificar que el Studio permite publicar con y sin referencia

## 2. Seed, dataset local y migración

- [x] 2.1 Actualizar `studio/scripts/seed-docs.ts` y `seed-data/eredita.ts` al nuevo modelo: singleton recortado con `projects`, documento `proyecto-eredita-art` (`order: 1`, `card` desde hero/intro) con `images[0]` por tipología; en `seed-local.ts` fabricar un `sanity.fileAsset` `.mp4` y asignarlo (con póster) a la tipología de áreas comunes (D8); verificar que `node studio/scripts/seed-local.ts` regenera `studio/.local/dataset.json` con ambos documentos y `npm run seed` sigue tipando (`npm --prefix studio run build`)
- [x] 2.2 Escribir `studio/scripts/migrate-proyectos.ts` (D8): lee el `eredita` publicado, crea `proyecto-eredita-art` sólo si no existe, transforma `image` → `images[0]`, hace `unset` de `gallery`/`typologies`/`legal` y `setIfMissing` de `projects` en la misma transacción; verificar contra un dataset de prueba (o `--dry-run` que imprime la transacción) que la segunda ejecución no escribe nada y que ninguna clave `_key` se pierde

## 3. Consultas, rutas y diccionario

- [x] 3.1 En `src/lib/content.ts` (D2/D3/D5): recortar `Eredita`/`getEredita`, añadir `Proyecto`, `getProyectos(lang)` (`name`, `slug`, `order`, `status`, `card`, `updatedAt`, ordenado por `order`) y `getProyecto(lang, slug)` con la proyección `media[]` ordenada (video primero, luego imágenes) y `getLegalDocs(lang, projectId)` con el filtro de referencia; verificar con `SANITY_LOCAL_DATASET` que un script imprime el proyecto en `en` con fallback al español y que `getLegalDocs('es', 'proyecto-eredita-art')` devuelve los documentos comunes
- [x] 3.2 Añadir `projectPath(lang, slug)` en `src/i18n/routes.ts` y las claves `eredita.viewProject`, `backToLine`, `projectTitle`, `media.{video,play,prev,next,aria}` en `es.ts` y `en.ts` (D9); verificar que `astro build` falla si se omite una clave en `en.ts` y pasa con ambas
- [x] 3.3 Crear `src/pages/eredita/[slug].astro` y `src/pages/en/eredita/[slug].astro` con `getStaticPaths` desde `getProyectos` (D2); verificar que un build offline produce `dist/eredita/eredita-art/index.html` y `dist/en/eredita/eredita-art/index.html`

## 4. Vistas

- [x] 4.1 Convertir `src/views/eredita.astro` en la portada de la línea: hero con marca (sin cambios), intro, sección Proyectos con una tarjeta por proyecto (imagen `CmsImg`, nombre, estado, texto, enlace `projectPath`) que se ve completa con un solo proyecto, y cierre; estilos en `eredita.css`; verificar en `/eredita/` y `/en/eredita/` a 390 y 1440 px que la tarjeta enlaza a `/eredita/eredita-art/` (y `/en/…`) y que la navegación del header no cambia
- [x] 4.2 Crear `src/views/proyecto.astro` con el layout actual (hero, intro, galería, tipologías, documentación, cierre) leyendo `getProyecto`, enlace "Volver a Ereditá", `SiteHead` con `page="eredita"`, `alternate` por slug, `title`/`description` recortados, `image={ogUrl(hero.poster)}` y `breadcrumbTitle`; verificar que el HTML de `/eredita/eredita-art/` conserva las secciones que tenía `/eredita/` y que `<head>` lleva canónica, `hreflang` recíproco, `x-default`, `og:image` absoluta y `BreadcrumbList` de tres ítems
- [x] 4.3 Galería en el panel de tipología (D6/D7): todos los medios pre-renderizados en `.ed-h-media` (video `preload="none"` con póster, imágenes `CmsImg` con `hidden`+`loading="lazy"` salvo la por defecto), tira `role="tablist"` con miniaturas `w≤320`, indicador de video, prev/next, omitida con un solo medio, superpuesta con altura fija; verificar en desktop y móvil que la altura de las tarjetas no varía entre tipologías con 1 y con 5 medios y que ninguna miniatura se solicita con `w>320`
- [x] 4.4 Escribir `src/scripts/typology-media.ts` y montarlo desde `eredita-motion.ts` (D6): conmutación por tabs (click, Enter/Espacio, flechas sin propagar a la pista), `IntersectionObserver` que reproduce el video del panel visible y pausa el resto, sin autoplay con `prefers-reduced-motion` (botón Reproducir); verificar con Playwright que al seleccionar el tercer medio el `<img>` visible cambia y el foco permanece en su tab, que al avanzar el track pinado el video anterior queda `paused`, que sin llegar a Tipologías no se solicita ningún `.mp4`, y que con `reduce` el video sólo arranca al pulsar el botón
- [x] 4.5 Enumeración de proyectos en `sitemap.xml.ts` (con `lastmod` del proyecto y alternativas) y en `llms.txt.ts` (línea por proyecto bajo Ereditá en cada idioma) (D3); verificar que `npm run build` (con `check-dist` y `seo-audit` en `postbuild`) pasa y que el sitemap y `dist/` coinciden

## 5. Tests y verificación integral

- [x] 5.1 Crear `tests/routes.mjs` (rutas estáticas + carpetas de proyecto en `dist/`) y usarlo en `seo-audit.mjs`, `i18n-sweep.mjs`, `mobile-audit.mjs` (`trackChecks` sobre el primer proyecto, con el total de tipologías del dataset local) y `mobile-perf.mjs` (`tracks` de proyecto); añadir a `mobile-audit` las comprobaciones de altura uniforme con distinto número de medios y de swipe sobre la tira sin cambiar la tarjeta encajada; verificar `npm run build`, `test:seo`, `test:i18n`, `test:mobile` y `test:perf` en verde con el dataset local
- [x] 5.2 Comprobar el presupuesto de JS móvil en `/eredita/eredita-art/` (`test:perf` ≤ 10 KB gzip de primera parte) y el frame budget durante el swipe; si se supera, recortar prev/next a CSS antes que la reproducción; verificar el informe en `tests/.artifacts`
- [x] 5.3 Ejecutar `test:sweep` y `contacto-sweep` para confirmar que home, header y 404 siguen apuntando a `/eredita/`; verificar que la escena Ereditá del home lleva a la portada

## 6. Despliegue y migración en producción

- [x] 6.1 `npm run studio:deploy` con el schema nuevo y ejecutar `npx sanity exec scripts/migrate-proyectos.ts --with-user-token` contra `production` (D8, plan de migración); verificar en el Studio que "Proyectos Ereditá → EREDITÁ Art" tiene las cuatro tipologías con su imagen en `images[0]`, la galería y el encabezado de documentación, y que "Ereditá (línea)" no muestra campos desconocidos
- [x] 6.2 Push a `main` con confirmación del usuario y sin líneas de atribución; verificar en producción `/eredita/`, `/eredita/eredita-art/`, `/en/eredita/eredita-art/`, `sitemap.xml` y `llms.txt`, y que el selector de idioma va de `/eredita/eredita-art/` a `/en/eredita/eredita-art/`
- [x] 6.3 Prueba de humo con el cliente: subir un mp4 comprimido con póster y dos renders a "Áreas comunes", crear un proyecto de prueba "EREDITÁ 2" en borrador y comprobar que no aparece hasta publicarlo; verificar que tras publicar el video se reproduce por defecto en el panel y que el render se selecciona desde la miniatura
- [x] 6.4 Webhook `vercel-deploy` en Sanity (dataset `production`, publicados, sin assets) → Deploy Hook de Vercel: cada Publish reconstruye el sitio
