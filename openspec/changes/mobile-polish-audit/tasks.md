## 0. Pipeline de imágenes en Cloudflare (`static-image-pipeline`)

- [x] 0.1 Pedir al usuario el log del último build en Cloudflare (Workers & Pages → diputnam → Deployments → Build log) y registrar en `design.md › Context` la versión de Node, el comando de build/deploy y cualquier aviso sobre `sharp`/servicio de imágenes; verificar que la causa registrada explica las URLs `/_image?`
- [x] 0.2 Versionar `wrangler.jsonc` en la raíz (`name: diputnam`, `compatibility_date`, `assets: { directory: "./dist", not_found_handling: "404-page" }`, sin `main`) y añadir `.wrangler/` a `.gitignore`; verificar que `npx wrangler deploy --dry-run` no ejecuta el auto-setup (`astro add cloudflare`) y que `npm run build` local sigue produciendo `dist/_astro/*.webp` sin ninguna referencia `/_image?` en `dist/**/*.html`
- [x] 0.3 Crear `tests/check-dist.mjs` y engancharlo como `"postbuild"` en `package.json`: falla con código ≠ 0 si algún `dist/**/*.html` contiene `/_image?` o referencia un `/_astro/*.{webp,avif,png,jpg}` inexistente; verificar que falla al inyectar a mano una referencia `/_image?` en un HTML de prueba y que pasa en el build real
- [x] 0.4 Commit de 0.2–0.3, push a `dev` (con confirmación del usuario), esperar el deploy de Cloudflare y verificar en su log que ya no aparece "Configuring project for Astro" ni `adapter: @astrojs/cloudflare`, y con `curl -I` que el logo del header, la marca de Ereditá y el hero de Únete responden `200 image/*` y que `curl -s <url>/unete/ | grep -c "/_image?"` = 0

## 1. Base de auditoría

- [x] 1.1 Crear `tests/mobile-audit.mjs` siguiendo el patrón de `tests/home-sweep.mjs` (BASE_URL, executablePath, `node:assert`) con el esqueleto por ruta a 390×844/DPR 2 y capturas en `tests/.artifacts/mobile/` (añadir a `.gitignore`); añadir `"test:mobile": "node tests/mobile-audit.mjs"` a `package.json` y verificar que `npm run test:mobile` corre contra `astro preview` y hoy **falla** en los checks de fondo café, hero de Putnam y pistas (línea base roja)

## 2. Fondo base y theme-color (`site-shell-theme`)

- [x] 2.1 Crear `src/components/SiteHead.astro` (charset, viewport `width=device-width, initial-scale=1, viewport-fit=cover`, icon, preload de fuente, `theme-color` claro `#f4eedf` y oscuro `#003a36` con `media`) y usarlo en las seis páginas; verificar con `grep -c theme-color dist/**/index.html` = 2 por página tras build
- [x] 2.2 Sustituir `#594037` en `:root`, `html` y `body` de `global.css` por `var(--putnam-cream)` y añadir `background: #0d1116` a `.home-stage`/`.home.is-enhanced .home-stage`; verificar con Playwright que `getComputedStyle(html|body).backgroundColor` ≠ `rgb(89, 64, 55)` en las seis rutas y que el overscroll superior de `/putnam/` (captura con `scrollTo(0,-1)` no aplica; usar captura del top del documento) no muestra café
- [ ] 2.3 Verificar en el Android del usuario que la barra de direcciones/sistema de Chrome adopta crema (o verde en modo oscuro) en home y en `/putnam/`

## 3. Home: franja entre paneles (`home-ui-motion-readability`)

- [x] 3.1 En `src/scripts/motion.ts`, leer `viewportHeight` de `stage.getBoundingClientRect().height` (stage = `[data-stage]`) en `refresh()` y en un `ResizeObserver` sobre el stage; usar ese valor en `panelTravel`/`titleTravel` y forzar `ScrollTrigger.update()` tras cada cambio; verificar que el check de home de `mobile-audit.mjs` (inyección de `svh = innerHeight − 90`, `gap === 0`, café < 0.3 %) pasa
- [x] 3.2 Añadir `height: 100%` explícito a `.home.is-enhanced .panel` y verificar en desktop (1440×900) con `npm run test:sweep` que el barrido de escenas y el título persistente no cambian

## 4. Logos (`brand-marks`)

- [x] 4.1 Preguntar al usuario si existe el isotipo Putnam en SVG. **Si sí:** guardarlo como `src/assets/putnam-mark.svg` con `fill="currentColor"`, renderizarlo inline en `SiteHeader.astro` y eliminar el intercambio de `src` en `menu.ts` (el color lo da `.site-header`/`--light`). **Si no:** generar con `getImage` variantes WebP + PNG a 360 px (3×) para dark y light, renderizar dos `<picture>` (dark/light) y alternar con `hidden` desde `menu.ts`. Verificar con Playwright que el `<img>` visible del header tiene `naturalWidth ≥ 300` (o hay `<svg>`) y que el tema sigue alternando al cruzar `.institutional-hero` → `.process` en `/putnam/`
- [x] 4.2 Recortar `src/assets/eredita-logo.png` a su caja de dibujo con 4 % de margen (script sharp de un solo uso, no se versiona), reemplazar el asset y cambiar `<Image>` por `<Picture formats={['webp']} fallbackFormat="png">` en `eredita.astro` (hero) y en el CTA de Ereditá; ajustar `.ed-hero-mark` (`width`/`right`) en `eredita.css` para móvil y desktop; verificar con captura a 390 px que la marca completa queda dentro del viewport con ≥ 1 rem de margen y en 1440 px conserva la composición
- [x] 4.3 Subir `.cta-mark` a `opacity: 0.32` en `putnam.css`, `unete.css`, `contacto.css`, `noticias.css` y declarar `overflow: hidden` en cada sección CTA; verificar con captura que el isotipo se distingue sobre `#004b46`, que el texto del CTA conserva contraste y que `scrollWidth === clientWidth` en las cinco rutas a 320 y 390 px

## 5. Putnam móvil: hero y reveals (`mobile-page-motion`)

- [x] 5.1 En `putnam-motion.ts`, eliminar la bifurcación por `mobileViewport`: la rama nativa queda sólo para `reducedMotion`; en móvil se importa GSAP/ScrollTrigger sin instanciar Lenis y se reutilizan `gsap.from` del hero, `[data-reveal-group]`, `processMarkers` y el rail; envolver el parallax de `[data-parallax]` y `.cta-mark` en `gsap.matchMedia('(min-width: 768px)')`; verificar que a 390 px el `h1` del hero tiene `opacity < 1` a los 80 ms y `=== 1` a los 1500 ms y que `body` no lleva `is-native-motion`
- [x] 5.2 Verificar que Proceso sigue funcionando en móvil (contador `01`→`06` y rail hasta `scaleX(1)` al llegar al final de `[data-process-markers]`) y que con `prefers-reduced-motion: reduce` emulado todo el contenido de `/putnam/` está visible sin animación (`opacity === 1` en hero y grupos)
- [x] 5.3 Medir arranque de `/putnam/` en móvil antes/después (Performance de DevTools o `page.metrics()` en Playwright, mismo guion de `openspec/changes/mobile-60fps/performance-notes.md`) y registrar que no empeora más de un 10 % el tiempo hasta hero visible

## 6. Pistas horizontales móviles (`mobile-horizontal-tracks`)

- [x] 6.1 Crear `src/scripts/horizontal-track.ts` exportando `mountHorizontalTrack({ track, cards, current, rail, total, reducedMotion })` → `unmount`: `IntersectionObserver` (root = track, umbral 0.6, desempate por cercanía al centro), `scroll` + rAF que actualiza `--progress` del rail de forma continua, teclado (`tabindex=0`, flechas → `scrollBy`), atributos ARIA (`role="group"`, `aria-roledescription`, `aria-label` por tarjeta, `aria-live` en contador); verificar con un test unitario ligero en Playwright (página `/unete/` a 390 px) que `scrollBy` al final deja contador `04 / 04` y `--progress` = 1
- [x] 6.2 Ereditá: en `eredita.astro` duplicar la intro como `<header class="ed-h-head">` (kicker, h2, intro, contador `data-track-current`/total, rail `data-track-rail`) visible sólo `< 900px`, y marcar la tarjeta intro del track como sólo `≥ 900px`; en `eredita.css` (`< 900px`) convertir `.ed-h-track` en pista `flex` con `overflow-x: auto`, `scroll-snap-type: x mandatory`, `scroll-padding-inline: var(--gutter)`, `overscroll-behavior-x: contain`, tarjetas `flex: 0 0 min(86vw, 26rem)`, `scroll-snap-align: center` y `::after` de gutter final; montar el helper en `eredita-motion.ts` dentro de `gsap.matchMedia('(max-width: 899px)')` con `ScrollTrigger.refresh()` tras montar/desmontar; verificar con captura a 390 px la composición (encabezado + pista) y `scrollWidth === clientWidth` a 320/390/768
- [x] 6.3 Únete: en `unete.astro` añadir `data-track-*` al `.pin-stage` (alias de `data-pin-*`) y a `.traits`; en `unete.css` (`< 900px`) convertir `.traits` en la misma pista `scroll-snap` y sustituir `.pin-rail span { width: 25% }` por `transform: scaleX(var(--progress, .25)); transform-origin: left`; montar el helper en `unete-motion.ts` dentro de `gsap.matchMedia('(max-width: 899px)')`; verificar con captura a 390 px que el encabezado (kicker, título, lead, contador, rail) queda encima de la pista sin espacio residual y que el contador/rail siguen el swipe
- [x] 6.4 Verificar sin regresión en desktop (1440×900): Tipologías sigue con pin + avance por rueda y Nuestra cultura con pin + rail por scroll (`npm run test:sweep` + captura manual de ambas secciones); verificar con `prefers-reduced-motion: reduce` emulado a 390 px que la pista sigue desplazable y el contador se actualiza sin `behavior: smooth`

## 7. Cierre

- [ ] 7.1 Completar `tests/mobile-audit.mjs` con todos los checks de D6 (fondo, theme-color, header ≥ 300 px o SVG, `.ed-hero-mark` dentro del viewport, franja de home, hero de Putnam, pistas) y verificar que `npm run build` (incluye `postbuild`) + `npm run preview` + `npm run test:mobile` + `npm run test:sweep` pasan en verde; correr `BASE_URL=https://diputnam.project0-0s.workers.dev npm run test:mobile` contra el deploy de preview y que pase
- [ ] 7.2 Revisión manual del usuario en su Android y escritorio, contra el deploy de Cloudflare, de las seis rutas (logos, heros de Únete/Contacto, barras del navegador, scroll de la home con barra retraída, hero de Putnam, swipe de Tipologías y Nuestra cultura) y registro de hallazgos pendientes, si los hay, como follow-ups en `design.md › Open Questions`

## 8. Hallazgos de la revisión en Android (deploy `1feb27b`)

- [x] 8.1 Home: el stage y sus paneles pasan a `100lvh` (con `100svh` como mínimo) para cubrir el viewport también con la barra de direcciones retraída; verificar con `svh = innerHeight − 90` simulado que no aparece ninguna franja del color del documento ni arriba ni abajo del stage en ningún punto del recorrido, y que `npm run test:sweep` sigue en verde
- [x] 8.2 Tipologías móvil: cada tarjeta ocupa el ancho del viewport menos los gutters (`scroll-snap-align: start`), todas con la misma altura (filas `auto 1fr`, contenido alineado arriba) y composición compacta (media más baja, paddings y specs en dos columnas); verificar con capturas a 320/390 que una tarjeta completa cabe a lo ancho, que la siguiente sólo asoma por el `gap` y que las cuatro miden lo mismo de alto
- [x] 8.3 Contacto móvil: la sección "motivos" usa la misma pista horizontal (`horizontal-track.ts`), contador y rail por `--progress` (también en desktop), y el correo de la lista de canales no desborda (`overflow-wrap: anywhere` + tamaño ajustado); verificar `scrollWidth === clientWidth` a 320/390 y contador `03 / 03` + rail al 100 % al final de la pista
- [x] 8.4 Menú móvil: la X de cierre queda centrada (desplazamiento = `gap + grosor` de las líneas, `calc(0.27rem + 1px)`); verificar con captura que ambas líneas se cruzan en el centro
- [x] 8.5 Sin resaltado azul al tocar: `-webkit-tap-highlight-color: transparent` y `touch-action: manipulation` en enlaces, botones, selects y controles del menú; verificar con `getComputedStyle(...).webkitTapHighlightColor` en `/contacto/` (botón, dropdown y enlaces) y en el menú
- [x] 8.6 Ampliar `tests/mobile-audit.mjs` con la pista de Contacto y la cobertura del stage en ambos estados de barra; `npm run build` + `npm run test:mobile` + `npm run test:sweep` en verde y commit en `dev`
