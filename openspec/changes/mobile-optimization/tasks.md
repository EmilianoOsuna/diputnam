## 1. Reveals y espaciado (causa raíz de los huecos)

- [x] 1.1 En `src/styles/global.css`, cambiar `.is-in [data-reveal]` por `.is-in[data-reveal], .is-in [data-reveal]` dentro del bloque `(max-width: 767px) and (prefers-reduced-motion: no-preference)`; verificar en Chrome a 390 px que en `/unete/` (Proceso de selección, Postulación espontánea, CTA) y `/contacto/` (Canales, Ubicación, Formulario, CTA) el párrafo bajo el título queda con `opacity: 1` tras hacer scroll y que el hueco desaparece.
- [x] 1.2 En `tests/mobile-audit.mjs`, añadir un check por ruta interior que, tras desplazar hasta el final, recorra `[data-reveal-group]` sin hijos elementales y falle si su `opacity` computada no es `1`; verificar que `npm run test:mobile` pasa (y que falla si se revierte 1.1).
- [x] 1.3 CTAs móviles en `(max-width: 767px)`: `unete.css` `.un-cta { min-height: 0; justify-content: flex-start }` y `.un-cta h2 { margin: 1.25rem 0 2rem }`; lo mismo para `.ct-cta` en `contacto.css` y `.nw-cta` en `noticias.css`; `putnam.css` `.institutional-cta { min-height: 0 }` y `.institutional-cta h2 { margin: 1.25rem 0 2.5rem }`; verificar a 390 px que en las cuatro rutas el kicker queda dentro del padding superior y el h2 empieza a < 2 rem del kicker, y que `.cta-mark` no tapa las acciones.
- [x] 1.4 Heros móviles: subir `line-height` del h1 a `0.98` en los bloques `(max-width: 767px)` de `putnam.css` (`.hero-copy h1`), `unete.css` (`.un-hero h1`), `noticias.css` (`.nw-hero h1`) y `contacto.css` (`.ct-hero h1`); confirmar que `eredita.css` (`1.05`) ya cumple; verificar en `/en/news/` a 390 px que la "g" de "progress" no intersecta la "i" del renglón siguiente (medir `getBoundingClientRect` de ambos `.line`) y que `npm run test:mobile` no reporta desborde de títulos.

## 2. Footer y menú móvil

- [x] 2.1 Crear `src/components/SocialLinks.astro` (props `lang`, `contact`) moviendo `ICON_PATHS` y el `map` de `socialLinks()` desde `ContactFooter.astro`; usarlo en las dos variantes del footer; verificar con `npm run build` que el HTML de `/` y `/putnam/` conserva los mismos `<a aria-label>` y paths que antes (diff de `dist/`).
- [x] 2.2 En `SiteHeader.astro`, dentro de `.mobile-menu-contact`, sustituir el enlace de texto "WhatsApp ↗" por `<SocialLinks>` y añadir el enlace `.footer-credit` (misma imagen y `aria-label` que el footer); verificar a 390 px, menú abierto, en `/` y `/en/` que se ven correo, teléfono, ciudad, iconos configurados y crédito, todos con foco visible y operables por teclado.
- [x] 2.3 En `global.css` bloque `(max-width: 767px)`: `.home-page .site-footer { display: none }` y estilos de la fila de iconos y crédito dentro de `.mobile-menu-contact` (iconos de 1.25 rem, gap 1.1 rem, color heredado); verificar en `/` a 390 px que no hay footer fijo, la flecha `.scroll-cue` sigue visible y funcional, y `document.documentElement.scrollWidth === innerWidth`.
- [x] 2.4 En `global.css` bloque `(max-width: 767px)`, reescribir el footer en flujo según design.md §8 (`.site-footer--flow` gap 1.75 rem y padding 2.75rem 1rem 2.25rem; `.footer-top` grid marca | redes con nav en fila completa; `.footer-bottom` contacto + crédito alineados); verificar a 320 y 390 px en `/putnam/`, `/contacto/` y `/eredita/` que las filas quedan separadas ≥ 1.5 rem, nada se pega, sin desbordamiento, y que a 1280 px el footer no cambia (captura antes/después).

## 3. Tipologías en pista horizontal (Ereditá)

- [x] 3.1 En `src/i18n/es.ts` y `en.ts`, añadir `eredita.details` ("Detalles" / "Details"); en `src/views/proyecto.astro`, envolver `.ed-h-desc` y `.ed-h-specs` en `<details class="ed-h-more" open><summary>{ui.eredita.details}</summary>…</details>`; verificar con `npm run build` y `npm run test:i18n` que ambas rutas de proyecto renderizan el details abierto.
- [x] 3.2 En `eredita.css`: en desktop `.ed-h-more > summary { display: none }` (y `list-style: none`); en `(max-width: 899px)` reescribir el bloque de Tipologías según design.md §2: sección `display: flex; flex-direction: column; min-height: 100dvh; height: 100dvh; padding-top: 5.5rem`, encabezado compacto (h2 más pequeño, intro con `-webkit-line-clamp: 2`), `.ed-h-progress` visible y estilizado como `.pin-counter`/`.pin-rail` de Únete (número 4 rem, total pequeño, rail debajo), `.ed-h-track` con los tokens de pista de `.traits` y `flex: 1; min-height: 0`, `.ed-h-panel` grid `minmax(0,1fr) auto` de ancho `calc(100vw - 2*var(--track-gutter))` con snap, `.ed-h-media { height: 100%; min-height: 0; aspect-ratio: auto }`, copy compacta y `summary` con estilo de `.ed-cta-link`; verificar a 390 × 844 px que la sección mide ≤ 844 px de alto con details cerrados, que el medio de la tarjeta encajada supera el 50 % de la altura de la tarjeta y que `scrollWidth === innerWidth`.
- [x] 3.3 En `src/scripts/eredita-motion.ts`, replicar el bloque de montaje de `unete-motion.ts`: con `matchMedia('(max-width: 899px)')` montar `mountHorizontalTrack({ track: [data-h-track], cards: [data-h-panel], current: [data-pin-current], rail: [data-pin-rail], label: 'Tipología' })`, y en el mismo `sync` quitar/poner `open` en todos los `details.ed-h-more`; verificar a 390 px que el contador va de `01` a `0N`, la barra llega a `scaleX(1)` en la última tipología, el details abre sin desencajar la pista, y a 1440 px los details están abiertos sin summary y `npm run test:eredita` sigue pasando.
- [x] 3.4 Verificar la convivencia con `typology-media.ts` a 390 px: el swipe sobre la tira de miniaturas desplaza la tira sin cambiar de tipología, el video de la tipología encajada se reproduce (o muestra el botón de play), y las flechas con foco en la tira mueven la selección, no la pista.
- [x] 3.5 En `tests/mobile-audit.mjs`, añadir `trackChecks(<ruta del primer proyecto en dist/>, '.ed-h-track', <n tipologías>)` reutilizando la resolución de ruta de `tests/eredita-gallery.mjs`, más un check de altura de sección ≤ `innerHeight` a 390 × 844; verificar que `npm run test:mobile` pasa.

## 4. Proceso Putnam en pista horizontal

- [x] 4.1 En `putnam.css` bloque `(max-width: 767px)`, sustituir la pila vertical según design.md §3: `.process-progress { display: flex; flex-direction: column; gap: 0 }` visible bajo el kicker, `.process-track` con los tokens de pista, `.process-panel` de ancho `calc(100vw - 2rem)` con snap y grid `auto auto` (`.process-media` `aspect-ratio: 4/3`, copia debajo), `.process-media-slot` sigue oculto; verificar a 390 px que las seis etapas se recorren con swipe, cada una muestra imagen o marcador pendiente + número + título + texto, y `scrollWidth === innerWidth`.
- [x] 4.2 En `src/scripts/putnam-motion.ts`, montar `mountHorizontalTrack` con `matchMedia('(max-width: 767px)')` (`track: [data-h-track]`, `cards: [data-h-panel]`, `current: [data-pin-current]`, `rail: [data-pin-rail]`, `label: 'Etapa'`), independiente de `prefers-reduced-motion`, desmontando al cruzar a ≥768 px; verificar a 390 px que el contador va de `01` a `06` y la barra llega a `scaleX(1)`, con `prefers-reduced-motion: reduce` sigue funcionando, y a 1280 px el pin de desktop no cambia.
- [x] 4.3 En `tests/mobile-audit.mjs`, añadir `trackChecks('/putnam/', '.process-track', 6)`; verificar que `npm run test:mobile` y `npm run test:perf` pasan (0 manejadores de `scroll` en `window`/`document` en `/putnam/`).

## 5. Verificación integrada

- [x] 5.1 `npm run build && npm run test:mobile && npm run test:perf && npm run test:eredita && npm run test:sweep && npm run test:i18n` sin fallas atribuibles al cambio.
- [x] 5.2 Capturas a 390 × 844 de: home (sin footer, flecha visible, menú abierto), footer en flujo de `/putnam/`, Tipologías de `/eredita/eredita-art/`, Proceso de `/putnam/`, Proceso de selección y CTA de `/unete/`, Canales/Ubicación/Formulario/CTA de `/contacto/`, hero de `/en/news/`; comparar con las capturas del reporte y confirmar que cada punto está resuelto.
- [x] 5.3 Capturas a 1280 px de home, `/putnam/`, `/eredita/eredita-art/`, `/unete/` y `/contacto/` comparadas con `main` para confirmar que desktop no cambió.
- [x] 5.4 `graphify update .` para refrescar el grafo.

## 6. Home: una escena por swipe (añadido durante la implementación)

- [x] 6.1 `motion.ts` `mountSwipe()`: `touch-action: pinch-zoom` en `body`, la escena sigue al dedo en `touchmove` y al soltar un tween rAF de 500 ms con curva `ease` (Swiper) aterriza en el marcador siguiente/anterior; flick < 300 ms siempre cambia, arrastre lento necesita media pantalla; clamp en los extremos; ignora el menú abierto. Verificado con toques sintéticos (flick → siguiente, arrastre corto → vuelve, tope sin rebote).
- [x] 6.2 `tests/mobile-perf.mjs`: en `/` y `/en/` se admiten ≤ 36 rAF por gesto (el tween) y 0 en reposo; `test:perf`, `test:mobile` y `test:sweep` en verde.
