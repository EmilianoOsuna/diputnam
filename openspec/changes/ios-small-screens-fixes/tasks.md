## 1. Flechas como icono

- [x] 1.1 Crear `src/components/Arrow.astro` (`dir: 'right' | 'up-right'`, `as: 'span' | 'i'`, envoltorio `aria-hidden="true"` con SVG `1em`/`currentColor`, trazo 1.4) y la regla única en `global.css` (`svg { display: block; vertical-align: -0.1em }` dentro del envoltorio); verificar que `npm run build` pasa y que el SVG se ve del mismo peso que el `→` actual en una captura a 1280 px de `/contacto/`.
- [x] 1.2 Sustituir cada `<span aria-hidden="true">→</span>`, `<span aria-hidden="true">↗</span>` y `<i aria-hidden="true">↗</i>` de `src/views/{contacto,unete,noticias,nota,putnam,eredita,proyecto}.astro` y `src/pages/404.astro` por `<Arrow dir=… as=…/>` conservando el elemento envoltorio de cada sitio; verificar con `grep -rn "→\|↗" src/views src/pages` que sólo quedan usos fuera de enlaces/botones (ninguno esperado) y que los hovers de `.cta-actions a span`, `.index-row i`, `.panel-button span`, `.f-submit button span`, `.hero-index span` y `.ed-cta-link span` siguen moviendo la flecha en desktop.
- [x] 1.3 Comprobar el texto accesible: `document.querySelector('.cta-actions a').textContent.trim()` no contiene el carácter de flecha y el nombre accesible del enlace (Playwright `getByRole('link', { name: 'Escríbenos' })`) resuelve; añadir el check a `tests/mobile-audit.mjs` (ver 6.3).

## 2. Slider de la home en iOS

- [x] 2.1 `global.css`: `html.is-slider, body.is-slider { height: 100%; overflow: hidden; overscroll-behavior: none }` (manteniendo `touch-action: pinch-zoom` en `body`) y `.home.is-slider .home-experience { touch-action: none }`; `motion.ts` añade `is-slider` también a `document.documentElement`. Verificar con `npm run test:mobile` que "home document does not scroll" sigue en verde a 844 y 754.
- [x] 2.2 `motion.ts`: `touchmove` con `{ passive: false }` y `preventDefault()` sólo mientras `dragging` y el toque empezó dentro de `[data-experience]`; `size` se lee en `touchstart` y en `visualViewport.resize`/`window.resize`. Verificar con toques sintéticos (`page.touchscreen` o `CDPSession Input.dispatchTouchEvent`) que un flick < 300 ms cambia de escena, que un arrastre lento < 50 % vuelve a la escena y que `scrollY` permanece en 0 durante el gesto; `npm run test:perf` sigue reportando 0 rAF/scroll en la home.
- [x] 2.3 `motion.ts` `slideTo()`: posición de reposo del stage en `%` (`-index * 100 / panels.length`), `.panel-media` en `±60%`, títulos en `∓60px`, `.panel-content` en `0`/`±100%`; el `render()` en px queda sólo para el arrastre y el congelado inicial. Verificar que la transición de 500 ms sigue ejecutándose en el compositor (sin saltos en el aterrizaje, `npm run test:sweep` y `npm run test:mobile` en verde) y que `getComputedStyle(stage).translate` en reposo coincide con `-index * innerHeight` ±1 px.
- [x] 2.4 Añadir a `tests/mobile-audit.mjs` el check "scene after viewport height change" (cargar a 390 × 754, ir a la escena 2 con la flecha, `setViewportSize(390 × 844)` y comprobar que el panel activo cubre el viewport y que `stage.style.translate` termina en `%`); verificar que falla contra `main` (posición en px) y pasa con el cambio.

## 3. Marca de agua de los CTA en móvil

- [x] 3.1 En el bloque móvil de `putnam.css`, `unete.css`, `contacto.css` y `noticias.css`: `.cta-mark { position: static; display: block; width: 72vw; margin: 2.5rem -1rem 0 auto }` y reducir el `padding-bottom` de `.institutional-cta` (7 → 4 rem), `.un-cta`, `.ct-cta` y `.nw-cta` (5 → 3.5 rem). Verificar a 390, 375 y 360 px que `mark.top > lastLink.bottom` en `/putnam/`, `/unete/`, `/contacto/`, `/noticias/` (y `/en/…`), que `scrollWidth === clientWidth` y que a 1280 px la marca sigue absoluta en la esquina inferior derecha (captura comparada con `main`).
- [x] 3.2 Confirmar que en `/putnam/` la marca sigue revelándose con el grupo `data-reveal-group` de la sección y que `content-visibility: auto` no recorta la marca (captura de la sección completa a 390 px).

## 4. Marca del hero de Ereditá en móvil

- [x] 4.1 `eredita.css` (móvil): `.ed-hero { flex-direction: column; justify-content: flex-end; align-items: stretch }`, `.ed-hero > picture { display: block; align-self: flex-end; margin: 0 0 clamp(1.5rem, 5vh, 3rem) }`, `.ed-hero-mark { position: static; top: auto; right: auto; width: 40vw; transform: none }`. Verificar en `/eredita/` y `/eredita/eredita-art/` a 375 × 635, 360 × 640 y 390 × 844 que el rectángulo de la marca no intersecta `.ed-back`, `.section-kicker`, `h1` ni `.ed-hero-sub`, que la marca sigue íntegra dentro del viewport con ≥ 16 px de margen (check existente) y que en desktop la posición no cambia.
- [x] 4.2 `tests/eredita-gallery.mjs`: check "hero mark never over text" a 1366 × 768 y 1512 × 982 (14") en `/eredita/` y en la página de proyecto (sin intersección entre `.ed-hero-mark` y `.ed-hero-content > *`); verificar en verde contra el estado actual (hoy ya se cumple) y que falla si se fuerza `width: 40vw` en la marca.
- [x] 4.3 Verificar que la entrada `m-fade-up` del hero y el parallax `[data-parallax]` del medio siguen funcionando (captura a los 200 ms y a los 1500 ms de carga) y que `npm run test:eredita` pasa.

## 5. Menú móvil opaco

- [x] 5.1 `global.css`: `.site-nav { background: var(--putnam-cream) }` en el bloque móvil. Verificar abriendo el menú sobre la home a 390 × 844 que un muestreo de píxeles del área del título del hero (`brownShare`-style sobre la captura) no detecta el crema-sobre-oscuro del título, y que `npm run test:sweep` (menú) y `npm run test:perf` (sin `backdrop-filter`) siguen en verde.

## 6. Auditoría y verificación

- [x] 6.1 `tests/mobile-audit.mjs`: pasada `shortViewports` (375 × 635 y 360 × 640) con helper `intersects()` que cubre 3.1 y 4.1 y el overflow horizontal de todas las rutas; verificar que la pasada falla contra `main` en los solapes y pasa con el cambio.
- [x] 6.2 `tests/mobile-audit.mjs` + `package.json`: selección del navegador por `PLAYWRIGHT_BROWSER` (`chromium` por defecto, `webkit` opcional) y script `test:mobile:webkit`; instalar WebKit con `npx playwright install webkit` (con `--with-deps` si faltan librerías) y ejecutar la pasada; si no puede instalarse en esta máquina, dejar el aviso claro en la salida del script y anotarlo aquí. _Nota (18 sep 2026): el binario de WebKit 2359 se descargó, pero el sistema no tiene sus librerías (libgtk-4, gstreamer, libsoup-3, libavif…); hace falta `sudo npx playwright install-deps webkit`. Mientras tanto `npm run test:mobile:webkit` termina con el aviso de instalación; la pasada WebKit queda pendiente de esa instalación._
- [x] 6.3 Check "action links use vector arrows" en la auditoría: ningún `a, button` contiene `→`/`↗` en su texto y todo `[aria-hidden] > svg` de flecha tiene `stroke: currentColor`; verificar en verde.
- [x] 6.4 `npm run build && npm run test:mobile && npm run test:perf && npm run test:eredita && npm run test:sweep && npm run test:i18n` sin fallas atribuibles al cambio (las 24 fallas de `test:perf` son el listener de scroll de `header-scroll.ts` en las páginas interiores y existen igual en `main`; la home sigue en 0 rAF/scroll); capturas a 1280 px de `/`, `/putnam/`, `/eredita/`, `/eredita/eredita-art/`, `/unete/`, `/contacto/`, `/noticias/` comparadas con `main` para confirmar que desktop no cambió.
- [ ] 6.5 Verificación en el iPhone real con la URL de preview de Vercel: reproducir las nueve capturas del reporte (CTA de Putnam, menú sobre la home, CTA de Ereditá, Únete, hero de proyecto, Contacto, home escena 2 → 4 con swipe y con la barra colapsada) y confirmar que no hay emoji, solapes ni franja de la escena anterior; anotar el modelo y la versión de iOS en este archivo.
