## 1. Prueba de rendimiento y baseline rojo

- [x] 1.1 Crear `tests/mobile-perf.mjs` (D8): contexto 390×844 `isMobile`/`hasTouch`/DPR 2, CDP `Emulation.setCPUThrottlingRate({ rate: 4 })`, sondas en `addInitScript` (envoltorio de `requestAnimationFrame` y de `addEventListener('scroll')` en `window`/`document` que cuenta callbacks del sitio sin contar los propios, muestreo de intervalos entre frames, `PerformanceObserver` de `longtask`), `Tracing` con `disabled-by-default-devtools.timeline` y gesto `Input.synthesizeScrollGesture` (`yDistance: -1600`, `speed: 1200`, ida y vuelta) por ruta; salida JSON por ruta con p50/p95, frames > 33 ms, long tasks, layouts forzados y callbacks; fallos acumulados y exit ≠ 0 según los umbrales de `mobile-rendering-performance`; añadir `"test:perf": "node tests/mobile-perf.mjs"` a `package.json` y eliminar `tests/putnam-performance.mjs`. Verificar que `npm run build && npm run preview` + `npm run test:perf` corre las seis rutas y hoy **falla** en todas por callbacks por frame (baseline rojo), guardando la salida en `tests/.artifacts/perf-baseline.json` (ignorado por git)
- [x] 1.2 Añadir a `tests/mobile-perf.mjs` un gesto horizontal (`xDistance`) sobre `[data-track]` en `/eredita/`, `/unete/` y `/contacto/` con umbral "≤ 1 callback por frame durante el gesto y 0 tras 300 ms de reposo"; verificar que hoy pasa (la pista ya está coalescida) y que sigue reportando métricas por pista

## 2. Cambios compartidos: header, scroll cue, imágenes, menú

- [x] 2.1 En `src/scripts/menu.ts` sustituir el listener de `scroll` + `getBoundingClientRect` por un `IntersectionObserver` sobre `[data-header-theme]` con `rootMargin: '-3% 0px -96% 0px'` (D6), recreado en `resize`; verificar en `mobile-audit.mjs` que el tema sigue cambiando al cruzar `.institutional-hero` → `.process` en `/putnam/` y que, con la sonda de 1.1, `/putnam/` ya no registra manejadores de `scroll` del documento
- [x] 2.2 En `src/lib/images.ts` añadir el parámetro `sat` para `images.unsplash.com` (D7) y aplicarlo en `index.astro` (paneles, `sat=-22`), `putnam.astro` (hero `-35`, Proceso `-38`), `eredita.astro` (hero `-35`, masonry `-8`, tipologías `-15`), `unete.astro`, `contacto.astro`, `noticias.astro` (heros `-35`, destacado `-15`); retirar los `filter:` CSS de `.panel-media`, `.hero-frame img`, `.process-media img`, `.ed-hero-media img`, `.ed-masonry-item img`, `.ed-h-media img`, `.nw-featured-media img` en todos los breakpoints y de `.ct-map iframe` en `< 768px`; verificar con Playwright que `getComputedStyle(img).filter === 'none'` en las seis rutas a 390 y 1440 px, que toda URL Unsplash del `dist` contiene `sat=`, y comparar capturas antes/después a 1440 px del hero de `/putnam/` y del panel 1 de `/` para confirmar tono equivalente
- [x] 2.3 En `global.css` (`< 768px`) quitar `backdrop-filter` de `.site-nav` y usar `background: rgba(244, 238, 223, 0.97)`; verificar con `home-sweep.mjs` (abrir/cerrar menú) y `getComputedStyle(nav).backdropFilter === 'none'`, y que el contraste de los enlaces del menú sobre el nuevo fondo sigue ≥ 4.5:1

## 3. Módulo móvil compartido y páginas interiores

- [x] 3.1 Crear `src/scripts/mobile-motion.ts` con `mountReveals(root, { exclude })` (D4: `data-reveal` + `--i` por hijo de `[data-reveal-group]`, un IO con `rootMargin: '0px 0px -18% 0px'`, añade `is-in` y deja de observar) y `supportsScrollTimeline()` (`CSS.supports('animation-timeline: view()')`); añadir en `global.css` la media query `(max-width: 767px) and (prefers-reduced-motion: no-preference)` con los estilos de `[data-reveal]`/`.is-in`, la `@keyframes` de entrada del hero escalonada por `--i` y `html { scroll-behavior: smooth }`; verificar con Playwright que a 390 px sin JS (`javaScriptEnabled: false`) el hero y los grupos de `/putnam/` terminan con `opacity === 1`
- [x] 3.2 Reestructurar `putnam-motion.ts`, `eredita-motion.ts`, `unete-motion.ts`, `contacto-motion.ts` y `noticias-motion.ts` según D1: mover el código desktop actual (Lenis, GSAP, ScrollTrigger, pins, parallax) a `src/scripts/desktop/<page>.ts` cargado con `import()` sólo cuando `min-width: 768px` y sin reduced motion; en móvil montar `mountReveals` y marcar los hijos del hero con `--i`; conservar el montaje de pistas horizontales por `matchMedia('(max-width: 899px)')`; verificar con `ls dist/_astro` que `gsap`, `ScrollTrigger` y `lenis` siguen siendo chunks separados y con Playwright que a 390 px ninguna de las cinco rutas solicita esos chunks y la suma de scripts propios transferidos es < 10 KB, mientras a 1440 px `npm run test:sweep` pasa
- [x] 3.3 Putnam móvil (D5): `timeline-scope: --process` en `.process`, `view-timeline-name: --process` en `.process-markers`, `@keyframes rail-grow` en `.process-rail span` con `animation-range: contain 0% contain 100%`; pasos activos con el IO de la antigua rama reduced-motion; el rAF de progreso sólo cuando `supportsScrollTimeline()` es falso; retirar la clase `motion-pending` del flujo móvil; verificar con Playwright a 390 px que el contador va de `01` a `06`, que al final de `[data-process-markers]` `getComputedStyle(span).transform` equivale a `scaleX(1)` y que a mitad del recorrido la escala es ≈ 0.5 leída en el mismo frame que `scrollTo`
- [x] 3.4 Ejecutar `npm run test:perf` y verificar que `/putnam/`, `/eredita/`, `/unete/`, `/contacto/` y `/noticias/` pasan todos los umbrales (0 callbacks rAF/scroll del sitio, p95 ≤ 20 ms, ≤ 1 long task, 0 layouts forzados durante el gesto) y `npm run test:mobile` sigue en verde

## 4. Home móvil en el compositor

- [x] 4.1 En `index.astro`/`global.css` implementar D2: `timeline-scope` con los nombres `--scene-n` en `.home-experience`, `view-timeline-name` en cada `.scene-marker[data-index]`, animación `scene-sweep` (0 % `translateY(100%)`, 50 % `0`, 100 % `translateY(-100%)`) en `.panel[data-index="n"]` con `animation-timeline: --scene-n; animation-range: entry 0% exit 100%; animation-fill-mode: both`, y `title-hold` inversa en `.panel-content` con `calc(∓100lvh ± 60px)`; entrada inicial (header/footer y stagger del copy intro) con `@keyframes` y `--i`; todo dentro de `(max-width: 767px)`; verificar con Playwright a 390 px que tras `scrollTo(0, 0.5 * marcador)` el panel 0 tiene `translateY(-50%)` y el panel 1 `translateY(50%)` leídos en el mismo `evaluate`, y que el título del panel 0 permanece a ≤ 30 px de su posición inicial
- [x] 4.2 En `motion.ts` (D1/D2/D3): en móvil no importar Lenis/GSAP; si `supportsScrollTimeline()`, añadir `is-enhanced` y montar un IO sobre los marcadores (`rootMargin: '-50% 0px -50% 0px'`) que aplique `is-active`/`aria-hidden`/`inert`/`pointer-events`; si no, añadir `is-stacked` (stage en flujo, paneles `height: 100svh; scroll-snap-align: start`, marcadores ocultos, `html { scroll-snap-type: y mandatory }`); scroll cue por IO sobre un centinela `[data-scroll-end]` y navegación con `scrollIntoView`; mover el motor desktop actual a `src/scripts/desktop/home.ts` con `import()`; verificar con Playwright que a 390 px `is-active` cambia exactamente al cruzar el 50 % de cada transición, que con `CSS.supports` forzado a falso (init script) la home muestra las cuatro escenas apiladas con `scroll-snap-type` en `html` y títulos visibles, y que a 1440 px `npm run test:sweep` pasa
- [x] 4.3 Ajustar `tests/mobile-audit.mjs` para los dos modos de la home (barrido CSS y apilado) manteniendo los checks de franja entre paneles y cobertura del stage con `svh = innerHeight − 90`; verificar `npm run test:mobile` en verde en ambos modos
- [x] 4.4 Ejecutar `npm run test:perf` y verificar que `/` pasa con 0 callbacks rAF/scroll del sitio durante el gesto vertical, p95 ≤ 20 ms y 0 layouts forzados; comparar contra `tests/.artifacts/perf-baseline.json` y registrar la mejora por ruta en el resumen del commit

## 5. Validación en dispositivo y cierre

- [ ] 5.1 `npm run build` (incluye `postbuild`), `npm run test:sweep`, `npm run test:mobile`, `npm run test:perf` en verde; commit por área (1–2, 3, 4) y push a `dev` con confirmación del usuario; esperar el deploy de Cloudflare y correr `BASE_URL=<preview> npm run test:mobile`
- [ ] 5.2 Revisión manual del usuario en su Android contra el deploy: scroll de las seis rutas pegado al dedo, barrido de la home, menú sin tirón al abrir/cerrar, pistas horizontales, Proceso de Putnam; registrar hallazgos como follow-ups en `design.md › Open Questions` o cerrar el change
